services:
  - type: web
    name: keycloak-dev
    env: docker
    plan: starter
    region: oregon 
    dockerfilePath: Dockerfile
    autoDeploy: true
    envVars:
      - key: KEYCLOAK_ADMIN
        value: admin
      - key: KEYCLOAK_ADMIN_PASSWORD
        value: admin
